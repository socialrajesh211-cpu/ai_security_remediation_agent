# Only needed in production, where the client and server are on different domains
# (e.g. client on Vercel/Netlify, server on Render). Point this at your deployed
# server's /api path. Leave unset for local dev — Vite's proxy handles it instead.
VITE_API_URL=https://your-server.onrender.com/api
